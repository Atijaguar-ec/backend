name: SSH connectivity check

on:
  workflow_dispatch:

jobs:
  ssh-check:
    runs-on: ubuntu-latest
    steps:
      - name: Test SSH connection
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TEST_ESPAM_HOST }}
          username: ${{ secrets.TEST_ESPAM_USER }}
          key: ${{ secrets.TEST_ESPAM_SSH_KEY }}
          script: |
            echo "SSH OK desde GitHub Actions para el entorno de pruebas"