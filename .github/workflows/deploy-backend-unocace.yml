# ============================================================================
# 🍫 Backend CI/CD - UNOCACE (Staging + Production)
# ============================================================================
# Este workflow despliega el backend para UNOCACE
# - Staging: inatrace-test.unocace.com/api
# - Production: inatrace.unocace.com/api
#
# Triggers:
# - staging branch → deploy to staging
# - main branch → deploy to production
# ============================================================================

name: 🍫 Backend UNOCACE

on:
  push:
    branches: [staging, main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend-unocace.yml'
  workflow_dispatch:
    inputs:
      environment:
        description: 'Target environment '
        required: true
        default: 'staging'
        type: choice
        options:
          - staging
          - production
      skip_quality:
        description: 'Skip quality checks '
        required: false
        default: false
        type: boolean

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository }}-inatrace
  JAVA_VERSION: '17'
  # UNOCACE specific
  COMPANY_NAME: UNOCACE
  PRIMARY_PRODUCT_TYPE: COCOA

jobs:
  # ============================================================================
  # Quality
  # ============================================================================
  quality:
    name: 🔍 Code Quality
    runs-on: ubuntu-latest
    if: ${{ !inputs.skip_quality }}
    permissions:
      contents: read
      checks: write
    services:
      mysql:
        image: mysql:8.0.35
        env:
          MYSQL_ROOT_PASSWORD: inatrace
          MYSQL_DATABASE: inatrace
          MYSQL_USER: inatrace
          MYSQL_PASSWORD: inatrace
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping -h 127.0.0.1 -uroot -proot" --health-interval=10s --health-timeout=5s --health-retries=10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: ${{ env.JAVA_VERSION }}
          distribution: 'temurin'
          cache: maven
      - name: 🔍 Run tests
        continue-on-error: true
        run: mvn clean test -B -Dspring.profiles.active=test

  # ============================================================================
  # Build
  # ============================================================================
  build:
    name: 🏗️ Build & Push
    runs-on: ubuntu-latest
    needs: [quality]
    if: always() && !cancelled()
    permissions:
      contents: read
      packages: write
    outputs:
      image-tag: ${{ steps.meta.outputs.tag }}
      image-ref: ${{ steps.image_ref.outputs.image }}
    steps:
      - uses: actions/checkout@v4

      - name: 🏷️ Generate metadata
        id: meta
        run: |
          if [[ "${GITHUB_REF_NAME}" == "main" ]]; then
            VERSION=$(grep -m1 '<version>' pom.xml | sed -E 's/.*<version>([^<]+)<\/version>.*/\1/' | sed 's/-SNAPSHOT//')
            TAG="unocace-v${VERSION}"
          else
            TAG="unocace-staging-${GITHUB_SHA::7}"
          fi
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "🏷️ Generated tag: $TAG"

      - name: Build with Maven
        run: mvn clean package -DskipTests

      - name: 🔐 Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - uses: docker/setup-buildx-action@v3

      - name: Compute image ref
        id: image_ref
        run: |
          IMAGE="${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}"
          IMAGE=$(echo "$IMAGE" | tr '[:upper:]' '[:lower:]')
          echo "image=$IMAGE" >> "$GITHUB_OUTPUT"

      - name: 🏗️ Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: |
            ${{ steps.image_ref.outputs.image }}:${{ steps.meta.outputs.tag }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: ✅ Build complete
        run: echo "📦 Image pushed - ${{ steps.image_ref.outputs.image }}:${{ steps.meta.outputs.tag }}"

  # ============================================================================
  # Deploy Staging
  # ============================================================================
  deploy-staging:
    name: 🧪 Deploy UNOCACE Staging
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/staging' || github.event.inputs.environment == 'staging'
    environment:
      name: unocace-staging
      url: https://inatrace-test.unocace.com
    steps:
      - uses: actions/checkout@v4

      - name: 🔐 Preflight
        run: |
          missing=0
          [ -z "${{ secrets.TEST_UNOCACE_HOST }}" ] && echo "❌ TEST_UNOCACE_HOST" && missing=1
          [ -z "${{ secrets.TEST_UNOCACE_USER }}" ] && echo "❌ TEST_UNOCACE_USER" && missing=1
          [ -z "${{ secrets.TEST_UNOCACE_PASSWORD }}" ] && echo "❌ TEST_UNOCACE_PASSWORD" && missing=1
          [ -z "${{ secrets.TEST_UNOCACE_DB_ROOT_PASSWORD }}" ] && echo "❌ TEST_UNOCACE_DB_ROOT_PASSWORD" && missing=1
          [ -z "${{ secrets.TEST_UNOCACE_DATASOURCE_PASSWORD }}" ] && echo "❌ TEST_UNOCACE_DATASOURCE_PASSWORD" && missing=1
          [ -z "${{ secrets.TEST_UNOCACE_ADMIN_PASSWORD }}" ] && echo "❌ TEST_UNOCACE_ADMIN_PASSWORD" && missing=1
          [ "$missing" -ne 0 ] && exit 1
          echo "✅ All secrets present"

      - name: 📁 Prepare remote directory
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          script: |
            sudo mkdir -p /opt/inatrace/backend
            sudo mkdir -p /opt/inatrace/import
            sudo chown -R $USER:$USER /opt/inatrace/backend
            sudo chown -R $USER:$USER /opt/inatrace/import
            sudo chmod -R u+rwX /opt/inatrace/import
            sudo rm -f /opt/inatrace/import/countries.csv || true

      - name: 📤 Upload docker-compose.yml
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          source: "ci/docker-compose.yml"
          target: "/opt/inatrace/backend/"
          strip_components: 1

      - name: 📤 Upload countries.csv
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          source: "import/countries.csv"
          target: "/opt/inatrace/backend/"
          strip_components: 1
          overwrite: true

      - name: 📁 Install countries.csv
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          script: |
            set -e
            sudo mkdir -p /opt/inatrace/import
            sudo cp -f /opt/inatrace/backend/countries.csv /opt/inatrace/import/countries.csv
            sudo chown $USER:$USER /opt/inatrace/import/countries.csv
            sudo chmod 0644 /opt/inatrace/import/countries.csv

      - name: 📝 Prepare .env
        run: |
          IMAGE="${{ needs.build.outputs.image-ref }}"
          TAG="${{ needs.build.outputs.image-tag }}"
          DB_USER="${{ secrets.TEST_UNOCACE_DATASOURCE_USERNAME }}"; [ -z "$DB_USER" ] && DB_USER="inatrace_test_unocace"
          FE_URL="${{ secrets.TEST_UNOCACE_FE_URL }}"; [ -z "$FE_URL" ] && FE_URL="https://inatrace-test.unocace.com"
          MAIL_HOST="${{ secrets.TEST_UNOCACE_MAIL_HOST }}"; [ -z "$MAIL_HOST" ] && MAIL_HOST="mail.atijaguar.com"
          MAIL_PORT="${{ secrets.TEST_UNOCACE_MAIL_PORT }}"; [ -z "$MAIL_PORT" ] && MAIL_PORT="465"
          MAIL_USER="${{ secrets.TEST_UNOCACE_MAIL_USERNAME }}"; [ -z "$MAIL_USER" ] && MAIL_USER="desarrollo@atijaguar.com"

          : "${ADMIN_PASSWORD:?Missing TEST_UNOCACE_ADMIN_PASSWORD}"
          cat > .env <<EOF
          IMAGE_NAME=$IMAGE
          TAG=$TAG
          DATABASE_NAME=inatrace_test_unocace
          DB_ROOT_PASSWORD=${{ secrets.TEST_UNOCACE_DB_ROOT_PASSWORD }}
          DATASOURCE_USERNAME=$DB_USER
          DATASOURCE_PASSWORD=${{ secrets.TEST_UNOCACE_DATASOURCE_PASSWORD }}
          CONTAINER_NAME_BE=inatrace-be-test-unocace
          CONTAINER_NAME_DB=inatrace-mysql
          HOST_PORT_BE=8080
          HOST_PORT_DB=3306
          FILE_STORAGE_ROOT=/opt/inatrace/uploads
          IMPORT_PATH=/opt/inatrace/import
          DOCUMENTS_ROOT=/opt/inatrace/documents
          FILE_STORAGE_ROOT_VOL=/opt/inatrace/uploads
          IMPORT_PATH_VOL=/opt/inatrace/import
          DOCUMENTS_ROOT_VOL=/opt/inatrace/documents
          DB_VOLUME=/opt/inatrace/mysql
          FE_URL=$FE_URL
          FE_CONFIRM_URL=${FE_URL}/confirm-email/
          FE_RESET_URL=${FE_URL}/reset-password/
          INATRACE_FLYWAY_IGNORE_MIGRATION_PATTERNS=*:missing
          SPRING_MAIL_HOST=$MAIL_HOST
          SPRING_MAIL_PORT=$MAIL_PORT
          SPRING_MAIL_USERNAME=$MAIL_USER
          SPRING_MAIL_PASSWORD=${{ secrets.TEST_UNOCACE_MAIL_PASSWORD }}
          SPRING_MAIL_SMTP_AUTH=true
          SPRING_MAIL_SMTP_SSL_ENABLE=true
          INATRACE_MAIL_SENDING_ENABLED=true
          INATRACE_AGSTACK_EMAIL=${AGSTACK_EMAIL:-inatrace@unocace.com}
          INATRACE_AGSTACK_PASSWORD=${{ secrets.TEST_UNOCACE_AGSTACK_PASSWORD }}
          INATRACE_ADMIN_EMAIL=${ADMIN_EMAIL:-inatrace@unocace.com}
          INATRACE_ADMIN_PASSWORD=${ADMIN_PASSWORD}
          INATRACE_ADMIN_NAME=${ADMIN_NAME:-Administrador}
          INATRACE_ADMIN_SURNAME=${ADMIN_SURNAME:-Unocace}
          INATRACE_ADMIN_COMPANY_NAME=${ADMIN_COMPANY_NAME:-UNOCACE}
          EOF
        env:
          AGSTACK_EMAIL: ${{ secrets.TEST_UNOCACE_AGSTACK_EMAIL }}
          ADMIN_EMAIL: ${{ secrets.TEST_UNOCACE_ADMIN_EMAIL }}
          ADMIN_PASSWORD: ${{ secrets.TEST_UNOCACE_ADMIN_PASSWORD }}
          ADMIN_NAME: ${{ secrets.TEST_UNOCACE_ADMIN_NAME }}
          ADMIN_SURNAME: ${{ secrets.TEST_UNOCACE_ADMIN_SURNAME }}
          ADMIN_COMPANY_NAME: ${{ secrets.TEST_UNOCACE_ADMIN_COMPANY_NAME }}

      - name: 📤 Upload .env
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          source: ".env"
          target: "/opt/inatrace/backend/"

      - name: 🚀 Deploy Staging 
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.TEST_UNOCACE_HOST }}
          username: ${{ secrets.TEST_UNOCACE_USER }}
          password: ${{ secrets.TEST_UNOCACE_PASSWORD }}
          script: |
            set -e
            cd /opt/inatrace/backend
            
            echo "🔗 Ensuring network... "
            docker network inspect inatrace-backend-network >/dev/null 2>&1 || docker network create inatrace-backend-network
            
            echo "📥 Pulling image..."
            docker compose pull backend
            
            echo "🛑 Stopping old containers..."
            docker stop inatrace-be-test-unocace 2>/dev/null || true
            docker rm inatrace-be-test-unocace 2>/dev/null || true
            
            echo "🚀 Starting..."
            docker compose up -d --no-deps --force-recreate backend
            
            echo "⏳ Health check..."
            for i in {1..12}; do
              sleep 10
              if curl -sf http://localhost:8080/actuator/health; then
                echo "✅ UNOCACE Staging deployed!"
                exit 0
              fi
              echo "  Attempt $i/12..."
            done
            
            echo "❌ Health check failed"
            docker logs inatrace-be-test-unocace --tail 50
            exit 1

  # ============================================================================
  # Deploy Production
  # ============================================================================
  deploy-production:
    name: 🚀 Deploy UNOCACE Production
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' || github.event.inputs.environment == 'production'
    environment:
      name: unocace-production
      url: https://inatrace.unocace.com
    steps:
      - uses: actions/checkout@v4

      - name: 🔐 Preflight
        run: |
          missing=0
          [ -z "${{ secrets.PROD_UNOCACE_HOST }}" ] && echo "❌ PROD_UNOCACE_HOST" && missing=1
          [ -z "${{ secrets.PROD_UNOCACE_USER }}" ] && echo "❌ PROD_UNOCACE_USER" && missing=1
          [ -z "${{ secrets.PROD_UNOCACE_PASSWORD }}" ] && echo "❌ PROD_UNOCACE_PASSWORD" && missing=1
          [ -z "${{ secrets.PROD_UNOCACE_DB_ROOT_PASSWORD }}" ] && echo "❌ PROD_UNOCACE_DB_ROOT_PASSWORD" && missing=1
          [ -z "${{ secrets.PROD_UNOCACE_DATASOURCE_PASSWORD }}" ] && echo "❌ PROD_UNOCACE_DATASOURCE_PASSWORD" && missing=1
          [ -z "${{ secrets.PROD_UNOCACE_ADMIN_PASSWORD }}" ] && echo "❌ PROD_UNOCACE_ADMIN_PASSWORD" && missing=1
          [ "$missing" -ne 0 ] && exit 1
          echo "✅ All secrets present"

      - name: 📁 Prepare remote directory
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          script: |
            mkdir -p /opt/inatrace/backend /opt/inatrace/import || true
            sudo mkdir -p /opt/inatrace/backend /opt/inatrace/import || true
            sudo chown -R $USER:$USER /opt/inatrace/backend /opt/inatrace/import || true
            sudo chmod -R u+rwX /opt/inatrace/import || true
            sudo rm -f /opt/inatrace/import/countries.csv || true

      - name: 📤 Upload docker-compose.yml
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          source: "ci/docker-compose.yml"
          target: "/opt/inatrace/backend/"
          strip_components: 1

      - name: 📤 Upload countries.csv
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          source: "import/countries.csv"
          target: "/opt/inatrace/backend/"
          strip_components: 1
          overwrite: true

      - name: 📁 Install countries.csv
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          script: |
            set -e
            sudo mkdir -p /opt/inatrace/import || true
            sudo cp -f /opt/inatrace/backend/countries.csv /opt/inatrace/import/countries.csv || true
            sudo chown $USER:$USER /opt/inatrace/import/countries.csv || true
            sudo chmod 0644 /opt/inatrace/import/countries.csv || true

      - name: 📝 Prepare .env
        run: |
          IMAGE="${{ needs.build.outputs.image-ref }}"
          TAG="${{ needs.build.outputs.image-tag }}"
          DB_USER="${{ secrets.PROD_UNOCACE_DATASOURCE_USERNAME }}"; [ -z "$DB_USER" ] && DB_USER="inatrace_prod_unocace"
          FE_URL="${{ secrets.PROD_UNOCACE_FE_URL }}"; [ -z "$FE_URL" ] && FE_URL="https://inatrace.unocace.com"
          MAIL_HOST="${{ secrets.PROD_UNOCACE_MAIL_HOST }}"; [ -z "$MAIL_HOST" ] && MAIL_HOST="mail.atijaguar.com"
          MAIL_PORT="${{ secrets.PROD_UNOCACE_MAIL_PORT }}"; [ -z "$MAIL_PORT" ] && MAIL_PORT="465"
          MAIL_USER="${{ secrets.PROD_UNOCACE_MAIL_USERNAME }}"; [ -z "$MAIL_USER" ] && MAIL_USER="desarrollo@atijaguar.com"

          : "${ADMIN_PASSWORD:?Missing PROD_UNOCACE_ADMIN_PASSWORD}"
          cat > .env <<EOF
          IMAGE_NAME=$IMAGE
          TAG=$TAG
          DATABASE_NAME=inatrace_prod_unocace
          DB_ROOT_PASSWORD=${{ secrets.PROD_UNOCACE_DB_ROOT_PASSWORD }}
          DATASOURCE_USERNAME=$DB_USER
          DATASOURCE_PASSWORD=${{ secrets.PROD_UNOCACE_DATASOURCE_PASSWORD }}
          CONTAINER_NAME_BE=inatrace-be-prod-unocace
          CONTAINER_NAME_DB=inatrace-mysql-prod-unocace
          HOST_PORT_BE=8080
          HOST_PORT_DB=3306
          FILE_STORAGE_ROOT=/opt/inatrace/uploads
          IMPORT_PATH=/opt/inatrace/import
          DOCUMENTS_ROOT=/opt/inatrace/documents
          FILE_STORAGE_ROOT_VOL=/opt/inatrace/uploads
          IMPORT_PATH_VOL=/opt/inatrace/import
          DOCUMENTS_ROOT_VOL=/opt/inatrace/documents
          DB_VOLUME=/opt/inatrace/mysql
          FE_URL=$FE_URL
          FE_CONFIRM_URL=${FE_URL}/confirm-email/
          FE_RESET_URL=${FE_URL}/reset-password/
          INATRACE_FLYWAY_IGNORE_MIGRATION_PATTERNS=*:missing
          SPRING_MAIL_HOST=$MAIL_HOST
          SPRING_MAIL_PORT=$MAIL_PORT
          SPRING_MAIL_USERNAME=$MAIL_USER
          SPRING_MAIL_PASSWORD=${{ secrets.PROD_UNOCACE_MAIL_PASSWORD }}
          SPRING_MAIL_SMTP_AUTH=true
          SPRING_MAIL_SMTP_SSL_ENABLE=true
          INATRACE_MAIL_SENDING_ENABLED=true
          INATRACE_AGSTACK_EMAIL=${AGSTACK_EMAIL:-inatrace@unocace.com}
          INATRACE_AGSTACK_PASSWORD=${{ secrets.PROD_UNOCACE_AGSTACK_PASSWORD }}
          INATRACE_ADMIN_EMAIL=${ADMIN_EMAIL:-inatrace@unocace.com}
          INATRACE_ADMIN_PASSWORD=${ADMIN_PASSWORD}
          INATRACE_ADMIN_NAME=${ADMIN_NAME:-Administrador}
          INATRACE_ADMIN_SURNAME=${ADMIN_SURNAME:-Unocace}
          INATRACE_ADMIN_COMPANY_NAME=${ADMIN_COMPANY_NAME:-UNOCACE}
          EOF
        env:
          AGSTACK_EMAIL: ${{ secrets.PROD_UNOCACE_AGSTACK_EMAIL }}
          ADMIN_EMAIL: ${{ secrets.PROD_UNOCACE_ADMIN_EMAIL }}
          ADMIN_PASSWORD: ${{ secrets.PROD_UNOCACE_ADMIN_PASSWORD }}
          ADMIN_NAME: ${{ secrets.PROD_UNOCACE_ADMIN_NAME }}
          ADMIN_SURNAME: ${{ secrets.PROD_UNOCACE_ADMIN_SURNAME }}
          ADMIN_COMPANY_NAME: ${{ secrets.PROD_UNOCACE_ADMIN_COMPANY_NAME }}

      - name: 📤 Upload .env
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          source: ".env"
          target: "/opt/inatrace/backend/"

      - name: 💾 Pre-deploy DB Backup
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          script: |
            CN=inatrace-mysql-prod-unocace
            if docker ps --format '{{.Names}}' | grep -q "^$CN$"; then
              echo "📦 Running DB backup..."
              BACKUP_DIR=/opt/inatrace/backups/mysql
              TS=$(date +%Y%m%d-%H%M%S)
              mkdir -p "$BACKUP_DIR"
              docker exec "$CN" sh -c 'mysqldump -u root -p"$MYSQL_ROOT_PASSWORD" "$MYSQL_DATABASE"' | gzip > "$BACKUP_DIR/backup-$TS.sql.gz"
              find "$BACKUP_DIR" -name "backup-*.sql.gz" -mtime +14 -delete || true
            else
              echo "⚠️ MySQL container not found, skipping backup"
            fi

      - name: 🚀 Deploy Production
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.PROD_UNOCACE_HOST }}
          username: ${{ secrets.PROD_UNOCACE_USER }}
          password: ${{ secrets.PROD_UNOCACE_PASSWORD }}
          script: |
            set -e
            cd /opt/inatrace/backend
            
            echo "🔗 Ensuring network..."
            docker network inspect inatrace-backend-network >/dev/null 2>&1 || docker network create inatrace-backend-network
            
            echo "📥 Pulling image..."
            docker compose pull backend || true
            
            echo "🚀 Deploying..."
            docker compose up -d --remove-orphans
            
            echo "⏳ Health check (local)..."
            for i in {1..30}; do
              sleep 10
              if curl -sf --max-time 5 http://localhost:8080/actuator/health; then
                echo "✅ Local health check OK"
                break
              fi
              echo "  Attempt $i/30..."
            done

            if ! curl -sf --max-time 5 http://localhost:8080/actuator/health; then
              echo "❌ Local health check failed"
              docker compose ps || true
              docker logs inatrace-be-prod-unocace --tail 200 || true
              exit 1
            fi

            echo "⏳ Health check (public)..."
            for i in {1..12}; do
              sleep 10
              if curl -sf --max-time 8 https://inatrace.unocace.com/api/actuator/health || curl -sf --max-time 8 https://inatrace.unocace.com/actuator/health; then
                echo "✅ UNOCACE Production deployed!"
                exit 0
              fi
              echo "  Attempt $i/12..."
            done

            echo "❌ Public health check failed"
            curl -sS --max-time 8 -D - https://inatrace.unocace.com/api/actuator/health | head -n 40 || true
            curl -sS --max-time 8 -D - https://inatrace.unocace.com/actuator/health | head -n 40 || true
            exit 1
