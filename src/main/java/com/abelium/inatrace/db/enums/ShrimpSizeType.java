package com.abelium.inatrace.db.enums;

/**
 * Enum for shrimp size types.
 * WHOLE = Head-On (Camarón entero con cabeza)
 * TAIL = Shell-On / Value Added (Cola)
 *
 * @author INATrace Team
 */
public enum ShrimpSizeType {
    WHOLE,
    TAIL
}
